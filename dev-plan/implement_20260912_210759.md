# implement_20260912_210759.md

작성 일시: `2026-09-12 21:07:59 KST`

상태: **Phase 1~4 구현·검증 완료**

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
로그인된 네이버 크리에이터 어드바이저의 최근 완료 7일 검색 유입 데이터와 메인 유입 제목 패턴을 읽어, 직전 7일과 비교한 근거 기반 블로그 키워드 추천 팩을 생성하는 naver-creator-keyword-recommender 스킬을 구현한다.

## 개발 범위
- `skills/naver-creator-keyword-recommender/` 신규 스킬과 `agents/openai.yaml`.
- 브라우저 관찰 런북, 입력·출력 계약, 점수·안전 기준 참고자료.
- 날짜·중복·증거 점수를 결정적으로 처리하는 Python 3.11 표준 라이브러리 CLI.
- 정상·부분 수집·경계 날짜·중복·민감도·제목 패턴 회귀 테스트.
- 최상위 README에 스킬 목적, 사용법, 연동 흐름만 추가.

## 제외 범위
- 네이버 비공개 API 호출, 쿠키·비밀번호 저장, CAPTCHA 우회, 공개 발행, 백그라운드 무인 크롤링, 검색 순위·유입 보장
- 타인의 제목 복제, 화면 숫자를 검색량으로 단정, 메인 유입 제목을 검색 키워드로 혼합

## 참조 문서
- skills/naver-title/SKILL.md
- skills/naver-series-workflow/SKILL.md
- skills/naver-smarteditor-drafter/SKILL.md
- /Users/hwanchoi/.codex/skills/.system/skill-creator/SKILL.md
- /Users/hwanchoi/.codex/skills/dev-plan-generator/SKILL.md

## 과거 실패·교훈 참조
- 검색 상태: `LESSON_SEARCH_COMPLETE`
- 검색 대상 scope: `skills/naver-creator-keyword-recommender`, `skills/naver-title`, `README.md`.
- 검색 결과: `0건`, warnings 없음. disposition을 적용할 정식 Dev Lesson은 없음.

## 전문가 검토 결정

| ID | 검토 결론 | 구현 통제 |
|---|---|---|
| R1 | 크리에이터 어드바이저는 로그인된 계정 화면이므로 공개 웹 검색이나 비공개 API로 대체하지 않는다. | 사용 가능한 브라우저 스킬로 읽기 전용 관찰만 하고 쿠키·자격증명을 파일에 저장하지 않는다. |
| R2 | `검색 유입 트렌드`는 주제 후보, `메인 유입 트렌드`는 제목 패턴이다. 서로 같은 키워드 풀로 합치면 의미가 훼손된다. | 입력 `surface`와 `kind`를 분리하고 메인 제목은 추천 키워드 점수에 넣지 않는다. |
| R3 | 최근성만으로 상승을 말할 수 없다. | 기본 추천 창은 완료된 D-1~D-7, 비교 창은 D-8~D-14로 고정한다. 비교 창이 없으면 상승 판정을 생략한다. |
| R4 | 화면의 `new`, 숫자, `-` 의미를 확인 없이 검색량으로 부를 수 없다. | `indicator_raw`로 보존하고 점수는 날짜별 등장·화면 목록 위치를 사용한다. |
| R5 | 지연 로딩, 로그인 만료, 비즈니스 화면 장애가 정상 실패 경로다. | 대기·스크롤·1회 재시도 후 `partial` 또는 `unavailable`로 기록하고 빈 데이터로 위장하지 않는다. |
| R6 | 원시 화면에는 계정·연령대·타인 제목 등 민감하거나 불필요한 정보가 포함될 수 있다. | 원시 스냅샷은 로컬 비커밋 경로에 두고 출력은 집계 중심으로 최소화한다. |
| R7 | 자극적인 메인 제목은 성과 보장이 아니며 루머·명예·금융 위험이 있다. | 구조만 추출하고 원문 복제 금지, 민감도 플래그와 사실 검증 게이트를 둔다. |
| R8 | 이 스킬은 상류 추천기이며 글 작성·UI 입력 책임을 가져오지 않는다. | 출력 `naver-creator-keyword-pack/v1`을 `naver-title` 등에 넘기고 SmartEditor는 명시 요청 때 기존 스킬로 연결한다. |

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 요구사항이 불명확하면 가능한 케이스를 나누고 확인 후 진행한다.
- 한 기능은 유지보수 가능한 최소 책임 단위로 나누고, 각 단위의 역할과 경계를 계획에 적는다.
- 계획 밖 옵션/확장/부가 기능을 만들기 위한 분리는 하지 않는다.
- 공통화는 계획 범위 안의 중복을 줄일 때만 수행하고 과도한 추상화는 피한다.
- 기존 프로젝트 API, 공식 SDK, 표준 라이브러리를 우선 검토하고 새 의존성은 범위상 필요할 때만 포함한다.
- material impact·재발 위험·QA escape·보안/개인정보/무결성 near-miss·큰 재작업 중 하나인 문제만 현재 Phase에 사실 기반 후보로 기록하고, 원인은 해결·검증 후 확정한다.
- 개발 종료 전 각 후보를 `plan-only | existing-reference | new-lesson` 중 하나로 분류한다.

## Phase 상태 요약
- [x] Phase 1. 전문가 검토와 계약 확정 완료
- [x] Phase 2. 스킬 골격과 지침 구현 완료
- [x] Phase 3. 근거 집계 CLI와 테스트 구현 완료
- [x] Phase 4. 통합 문서·검증·완료 감사 완료

## QA 관점
- [x] D-1/D-7/D-8/D-14 경계와 미래·오늘 데이터 제외를 검증한다.
- [x] 같은 날짜·여러 섹션의 중복이 등장일 수를 부풀리지 않는지 확인한다.
- [x] 최근 또는 비교 데이터 부족 시 `rising`을 만들지 않고 신뢰도를 낮추는지 확인한다.
- [x] 메인 제목이 추천 키워드 배열에 섞이지 않는지 확인한다.
- [x] `new`, 숫자, `-`를 검색량으로 변환하지 않는지 확인한다.
- [x] 빈 배열과 수집 실패를 구분하고 partial 사유를 보존하는지 확인한다.
- [x] 타이틀 패턴 분류가 원문 성과·사실성을 주장하지 않는지 확인한다.
- [x] 기존 전체 테스트와 스킬 정적 검사를 다시 실행한다.

## 교훈 분류 요약
- [x] 발생 후보 없음
- 후보가 있으면 종료 전에 분류 결과와 Lesson ID 또는 plan-only 사유를 적는다.

---

## Phase 1. 전문가 검토와 계약 확정

### 목표
- 책임 경계와 데이터 의미를 확정하고, 구현이 네이버 내부 API 추측이나 제목 복제로 흐르지 않게 한다.

### 구현 태스크
- [x] R1~R8 검토 결정을 계약과 테스트 항목으로 연결한다.
- [x] `naver-creator-snapshots/v1` 입력과 `naver-creator-keyword-pack/v1` 출력을 정의한다.
- [x] 최근 7일·직전 7일·부분 수집의 상태 의미를 확정한다.
- [x] 추천 전용, 제목 연동, 전체 워크플로 연동의 책임을 분리한다.

### 자체 테스트
- [x] 입력 예시가 검색 키워드와 메인 제목을 명시적으로 구분한다.
- [x] 데이터 누락·지연 로딩·로그인 만료·페이지 장애 시 종료 상태가 정의되어 있다.
- [x] 메인 제목 원문을 그대로 추천하거나 성과를 보장하는 경로가 없다.

### 이슈 및 수정
- [x] 발견 이슈 없음
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 2. 스킬 골격과 지침 구현

### 목표
- 다른 Codex 인스턴스가 로그인 화면 관찰부터 추천 팩 전달까지 같은 안전 절차로 수행할 수 있게 한다.

### 구현 태스크
- [x] `init_skill.py`로 `scripts,references` 구조와 UI 메타데이터를 생성한다.
- [x] `SKILL.md`에 모드, 브라우저 관찰, 분석, 출력, 실패·개인정보·검증 규칙을 작성한다.
- [x] 데이터 계약, 브라우저 런북, 점수·안전 기준, 사용 예를 참고자료로 분리한다.
- [x] 비공개 원시 스냅샷 경로를 `.gitignore`에 추가한다.

### 자체 테스트
- [x] frontmatter에 name·description만 존재한다.
- [x] `agents/openai.yaml` 기본 프롬프트가 `$naver-creator-keyword-recommender`를 포함한다.
- [x] SKILL.md가 500줄 이내이며 모든 참고자료가 직접 연결된다.

### 이슈 및 수정
- [x] 발견 이슈 없음
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 3. 근거 집계 CLI와 테스트 구현

### 목표
- 브라우저에서 수집한 구조형 관찰값을 재현 가능한 근거 점수와 제목 패턴 통계로 변환한다.

### 구현 태스크
- [x] 입력 스키마·날짜·surface·item을 닫힌 방식으로 검증한다.
- [x] NFKC·공백·대소문자 기준으로 검색어를 정규화하고 날짜별 중복을 제거한다.
- [x] 최근/비교 창 등장일, 최근성, 화면 위치, 신규성을 근거 점수로 계산한다.
- [x] 비교 창 부족 시 상승 라벨을 차단하고 partial 근거를 출력한다.
- [x] 메인 제목은 인용·숫자·질문·갈등·반전·정보 공백 패턴만 집계한다.
- [x] 표준 라이브러리 `unittest` 회귀 테스트를 추가한다.

### 자체 테스트
- [x] 정상 14일 입력에서 지속·신규·상승 유형과 정렬을 확인한다.
- [x] 중복·경계 날짜·부분 수집·잘못된 스키마가 예상대로 처리된다.
- [x] 메인 제목이 키워드 결과에 들어가지 않고 집계에는 반영된다.
- [x] CLI 샘플 실행 결과가 JSON으로 재현된다.

### 이슈 및 수정
- [x] 발견 이슈 없음
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 4. 통합 문서·검증·완료 감사

### 목표
- 기존 제목·원고·SmartEditor 스킬과의 연결점을 문서화하고 저장소 회귀 없이 완료한다.

### 구현 태스크
- [x] 최상위 README에 스킬 소개, 범위별 프롬프트, 안전한 연동 흐름을 추가한다.
- [x] quick_validate와 신규·기존 테스트를 실행한다.
- [x] diff에서 비밀정보·계정 원시 데이터·캐시·불필요 파일 포함 여부를 감사한다.
- [x] 계획 체크박스와 실제 증거를 일치시키고 잔여 리스크를 기록한다.

### 자체 테스트
- [x] `quick_validate.py skills/naver-creator-keyword-recommender`가 통과한다.
- [x] `python3.11 -m unittest discover -s tests`가 통과한다.
- [x] README 예제가 추천만 실행할 때 브라우저 쓰기나 발행을 유발하지 않는다.
- [x] `git status`와 `git diff --check`로 변경 범위를 확인한다.

### 이슈 및 수정
- [x] 발견 이슈 없음
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## 최종 결과 요약

- 신규 스킬: `skills/naver-creator-keyword-recommender/`.
- 계약: `naver-creator-snapshots/v1` 입력을 `naver-creator-keyword-pack/v1` 집계로 변환.
- 책임 분리: 검색 유입은 키워드, 메인 유입은 제목 패턴으로 처리하고 후속 작성·SmartEditor는 기존 스킬에 위임.
- 개인정보 통제: 원시 관찰 데이터용 `.creator-advisor-private/`, `outputs/creator-advisor-private/`를 Git에서 제외.
- 신규 테스트: `tests/test_creator_keyword_recommender.py` 7개 통과.
- 전체 Python 회귀: 116개 통과.
- SmartEditor Node 회귀: 28개 통과.
- 스킬 정적 검사: `Skill is valid!`.
- 저장소 검사: `git diff --check` 통과, 실제 계정 ID·쿠키·토큰·원시 대시보드 데이터 미포함.

## 잔여 리스크 / 후속 과제

- 네이버 화면의 DOM·문구·메뉴는 변경될 수 있으므로 라이브 실행 때마다 런북에 따라 화면 의미를 먼저 확인한다.
- `비즈니스` 화면이 서비스 오류이거나 항목 의미가 불명확하면 `partial` 또는 `business_signal`로 남기며 자동 승격하지 않는다.
- 현재 구현은 브라우저를 직접 호출하는 상시 수집기가 아니다. 로그인된 UI 관찰은 실행 시 사용 가능한 브라우저 스킬이 담당한다.
- 저장소에는 구현했지만 전역 Codex 스킬 설치와 Git 커밋·푸시는 이번 요청 범위에서 수행하지 않았다.

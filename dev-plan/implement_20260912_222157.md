# implement_20260912_222157.md

작성 일시: `2026-09-12 22:21:57 KST`

상태: **Phase 1~3 구현·검증 완료**

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
naver-creator-keyword-recommender의 기본 추천 결과에 키워드별 대표 제목을 추가하되 본문·이미지·SmartEditor 작업은 수행하지 않도록 모드와 후속 연동 계약을 개선한다.

## 개발 범위
- `skills/naver-creator-keyword-recommender/SKILL.md` 기본 모드의 제목 생성 책임 변경
- 키워드별 대표 제목 1개를 만드는 `naver-title` 핸드오프 계약
- `keywords-only`, `patterns`, `title-candidates`, `full` 모드와 사용자 문구 해석 규칙
- 사용 예, `agents/openai.yaml`, 최상위 README의 기본 동작 동기화
- 스킬 정적 검사, Python 회귀 테스트, diff 검사

## 제외 범위
- 제목 문구를 Python CLI에서 자동 생성하는 기능
- 본문·이미지 생성, SmartEditor 조작, 공개·예약 발행
- 메인 유입 제목의 원문 복제, 미확인 수치·인용·성과 생성
- 기존 스냅샷·집계 스키마의 비호환 변경

## 참조 문서
- 이전 개발 계획: dev-plan/implement_20260912_210759.md
- skills/naver-creator-keyword-recommender/SKILL.md
- skills/naver-creator-keyword-recommender/references/examples.md
- skills/naver-title/SKILL.md
- README.md

## 과거 실패·교훈 참조
- 검색 상태: `LESSON_SEARCH_COMPLETE`
- 검색 대상 scope: `skills/naver-creator-keyword-recommender`, `skills/naver-title`, `README.md`, `tests/test_creator_keyword_recommender.py`
- 검색 결과: `0건`, warnings 없음. disposition을 적용할 정식 Dev Lesson은 없음.

## 확정된 행동 계약

| ID | 결정 | 구현 통제 |
|---|---|---|
| T1 | 기본 `recommend`는 키워드 10개와 키워드별 대표 제목 1개를 함께 출력한다. | 대표 제목은 `naver-title` 규칙으로 후보를 검수한 뒤 1개만 노출한다. |
| T2 | `본문/글/원고는 작성하지 마`는 제목을 제외하지 않는다. | 제목 제외는 `키워드만`, `제목도 제외`, `제목을 만들지 마`가 명시된 경우로 한정한다. |
| T3 | `메인 유입에서는 제목 패턴만 분석`은 원문을 복제하지 말라는 데이터 사용 제약이다. | 그 문구만으로 추천 제목 생성을 끄지 않는다. |
| T4 | 언어 생성은 모델·`naver-title`, 수집·집계는 기존 CLI가 담당한다. | `analyze_snapshots.py`의 현재 스키마와 결정적 점수는 변경하지 않는다. |
| T5 | 본문·이미지·SmartEditor는 계속 명시적 요청에서만 연결한다. | 기본 종료 지점을 `추천 표+패턴 요약`으로 고정한다. |

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 요구사항이 불명확하면 가능한 케이스를 나누고 확인 후 진행한다.
- 한 기능은 유지보수 가능한 최소 책임 단위로 나누고, 각 단위의 역할과 경계를 계획에 적는다.
- 계획 밖 옵션/확장/부가 기능을 만들기 위한 분리는 하지 않는다.
- 공통화는 계획 범위 안의 중복을 줄일 때만 수행하고 과도한 추상화는 피한다.
- 기존 프로젝트 API, 공식 SDK, 표준 라이브러리를 우선 검토하고 새 의존성은 범위상 필요할 때만 포함한다.
- material impact·재발 위험·QA escape·보안/개인정보/무결성 near-miss·큰 재작업 중 하나인 문제만 현재 Phase에 사실 기반 후보로 기록하고, 원인은 해결·검증 후 확정한다.
- 개발 종료 전 각 후보를 `plan-only | existing-reference | new-lesson` 중 하나로 분류한다.

## Phase 상태 요약
- [x] Phase 1. 행동 계약과 모드 결정 완료
- [x] Phase 2. 스킬·예시·UI 문서 구현 완료
- [x] Phase 3. 회귀 검증과 완료 감사 완료

## QA 관점
- [x] `본문 미작성`, `키워드만`, `제목도 제외`, `패턴만 분석` 문구의 경계를 검토했다.
- [x] 원문 복제, 미확인 수치·인용, 본문·SmartEditor 자동 실행 회귀를 검토했다.
- [x] 기존 집계 CLI와 116개 Python 테스트의 회귀가 없음을 확인했다.

## 교훈 분류 요약
- [x] 발생 후보 없음
- 후보가 있으면 종료 전에 분류 결과와 Lesson ID 또는 plan-only 사유를 적는다.

---

## Phase 1. 행동 계약과 모드 결정

### 목표
- 본문 금지와 제목 금지를 분리하고, 메인 유입 패턴을 안전한 제목으로 전환하는 책임 경계를 확정한다.

### 구현 태스크
- [x] 기본 `recommend`의 출력을 `키워드 10개+대표 제목 1개씩`으로 확정한다.
- [x] `keywords-only`, `patterns`, `title-candidates`, `full` 모드의 책임과 종료 지점을 확정한다.
- [x] `본문 제외` 문구와 `제목 제외` 문구의 우선순위를 확정한다.
- [x] 메인 유입 원문과 생성 제목 사이의 비복제·사실성 게이트를 확정한다.

### 자체 테스트
- [x] 제목 추천을 받고 본문은 생성하지 않는 요청을 표준 케이스로 정의했다.
- [x] `키워드만`, `제목도 제외`, `패턴만 분석` 경계 문구의 기대 행동을 정의했다.
- [x] 기존 Python 집계 계약에 스키마 변경이 필요하지 않음을 확인했다.

### 이슈 및 수정
- [x] 발견 이슈 없음
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 2. 스킬·예시·UI 문서 구현

### 목표
- 다른 Codex 인스턴스가 기본 요청에서 키워드별 대표 제목을 누락하지 않고, 제목 금지 요청은 정확히 존중하게 한다.

### 구현 태스크
- [x] `SKILL.md` 모드 표·추천 출력·후속 연결 절차를 수정한다.
- [x] 제목 핸드오프 상세 계약을 추가하고 `SKILL.md`에서 조건부로 연결한다.
- [x] `references/examples.md`에 기본·키워드만·복수 제목 예시를 추가한다.
- [x] `agents/openai.yaml`과 README의 기본 프롬프트·흐름을 동기화한다.

### 자체 테스트
- [x] 기본 프롬프트가 키워드별 제목과 본문 미작성을 동시에 요청하는지 확인한다.
- [x] 출력 표에 추천 제목·적용 패턴·위험도가 함께 정의되어 있는지 확인한다.
- [x] 타인 제목 원문 복제와 미확인 수치 생성을 금지하는지 확인한다.

### 이슈 및 수정
- [x] 발견 이슈 없음
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 3. 회귀 검증과 완료 감사

### 목표
- 스킬 패키지 유효성과 기존 집계 코드의 회귀가 없음을 확인하고 변경 범위를 감사한다.

### 구현 태스크
- [x] `quick_validate.py`와 스킬 링크·줄 수 검사를 실행한다.
- [x] 관련 테스트와 전체 Python 회귀 테스트를 실행한다.
- [x] `git diff --check`와 `git status` 기반으로 변경 범위·비밀정보·생성물을 감사한다.
- [x] 계획 체크박스와 최종 증거를 일치시킨다.

### 자체 테스트
- [x] `python3.11 -m unittest discover -s tests -p 'test_creator_keyword_recommender.py'`가 7개 통과한다.
- [x] `python3.11 -m unittest discover -s tests`가 116개 통과한다.
- [x] `quick_validate.py skills/naver-creator-keyword-recommender`가 `Skill is valid!`로 통과한다.
- [x] README·사용 예·UI 기본 프롬프트의 행동이 서로 일치한다.

### 이슈 및 수정
- [x] `tests`가 Python 패키지가 아니어 모듈 지정 명령이 실패했고, 상품 코드 변경 없이 `unittest discover -p`로 검증 명령만 정정했다. 재발 위험·사용자 영향이 없어 Dev Lesson 후보로는 분류하지 않았다.
- 발생 후보 형식: `OCC-<id> | 시각 | 증상 | 영향 | 재현/증거 | 임시조치` (원인 추정 금지)

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## 최종 결과 요약

- 기본 `recommend`를 `키워드+키워드별 대표 제목 1개`로 변경했다.
- `본문/글/원고는 작성하지 마`는 본문만 막고, `키워드만/제목도 제외`가 제목을 막도록 문구 우선순위를 고정했다.
- 새 `references/title-handoff.md`에 입력 패킷, `naver-title` 검수, 원문 비복제, 수치·인용·정보 공백 제약을 정의했다.
- 사용 예, UI 기본 프롬프트, README 흐름을 새 기본 동작과 동기화했다.
- 수집·집계 CLI와 JSON 스키마는 변경하지 않았다.

## 잔여 리스크 / 후속 과제

- 제목은 추후 작성할 본문의 약속이므로, 본문을 요청받으면 수치·일정·규정을 다시 검증해야 한다.
- 현재 변경은 스킬 행동 계약이며, 제목 문구를 Python CLI에서 결정적으로 생성하지 않는다.
